name: FitStrive Server
on:
  push:
    branches:
      - main

jobs:
  Update-Server:
    runs-on: self-hosted

    defaults:
      run:
        working-directory: /home/fitstrive/server
  
    steps:
      - name: Checkout server directory
        uses: actions/checkout@v6
        with:
          sparse-checkout: |
            server
          
      - name: Deploy
        run: |
          echo "Prepping server for deployment!"
          npm ci
          npm run build
          pm2 restart fitstrive-server
